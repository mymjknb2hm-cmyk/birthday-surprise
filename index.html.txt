<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Happy Birthday 🎉</title>

<style>
  body {
    margin: 0;
    background: black;
    height: 100vh;
    overflow: hidden;
    font-family: Arial, sans-serif;
  }

  video {
    width: 100%;
    height: 100vh;
    object-fit: cover;
    cursor: pointer;
  }

  #overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,0.7);
    display: none;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    color: white;
    text-align: center;
    padding: 20px;
  }

  h1 {
    font-size: 36px;
    margin-bottom: 10px;
  }

  p {
    font-size: 18px;
    margin-bottom: 20px;
  }

  button {
    padding: 12px 20px;
    font-size: 16px;
    border: none;
    border-radius: 25px;
    background: #ff4d6d;
    color: white;
    cursor: pointer;
  }

  .confetti {
    position: absolute;
    width: 10px;
    height: 10px;
    animation: fall 3s linear infinite;
  }

  @keyframes fall {
    0% { transform: translateY(-100px); }
    100% { transform: translateY(100vh); }
  }
</style>
</head>

<body>

<video id="video" autoplay muted loop>
  <source src="birthday.mp4" type="video/mp4">
</video>

<div id="overlay">
  <h1>🎂 Happy Birthday 🎉</h1>
  <p>May your life be filled with happiness, love and success 💖</p>
  <button onclick="openWish()">Send Love 💌</button>
</div>

<script>
  const video = document.getElementById("video");
  const overlay = document.getElementById("overlay");

  video.addEventListener("click", () => {
    video.pause();
    overlay.style.display = "flex";
    createConfetti();
  });

  function openWish() {
    window.open(
      "https://wa.me/?text=Happy%20Birthday%20🎉🎂%20Stay%20happy%20always!",
      "_blank"
    );
  }

  function createConfetti() {
    for (let i = 0; i < 40; i++) {
      const confetti = document.createElement("div");
      confetti.className = "confetti";
      confetti.style.left = Math.random() * 100 + "vw";
      confetti.style.background = 
        `hsl(${Math.random() * 360}, 100%, 50%)`;
      confetti.style.animationDuration =
        Math.random() * 2 + 2 + "s";
      document.body.appendChild(confetti);
    }
  }
</script>

</body>
</html>